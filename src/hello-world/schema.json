{
  "$schema": "http://json-schema.org/schema",
  "id": "HelloWorldSchema",
  "title": "Menu Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "Name of who to say hi to. Default to 'world!'",
      "type": "string"
    },
    "path": {
      "description": "Where to create the file.",
      "type": "string",
      "format": "path"
    }
  }
}